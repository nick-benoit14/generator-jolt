import React from 'react';
import <%= componentName %> from '<%= componentFilePath %>';

describe('<%= componentName %>', () => {
  it('does the right thing', () => {
    expect(false).toBe(true);
  });
});
